<?php

/**
 * hook_ctools_plugin_directory().
 */
function badges_ctools_plugin_directory($module, $type) {

  // Load the export_ui plugin.
  if ($type =='export_ui') {
    return 'plugins/export_ui';
  }
}
